[project]
name = "djua-box-telemetry-api"
version = "0.1.0"
description = "FastAPI service that receives Djua Box MQTT telemetry."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115,<1.0",
    "paho-mqtt>=2.0,<3.0",
    "uvicorn[standard]>=0.30,<1.0",
]

[tool.uv]
package = false
